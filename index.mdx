---
title: "PES6 Game Physics Mod: Pass Power Enhancement"
sidebarTitle: "Home"
description: "A Win32 DLL plugin that intercepts PES6's physics integrator to dynamically rescale pass power, eliminating dead-pass inertia for fluid first-touch play."
---

PES6 Game Physics Mod is an open-source Win32 DLL plugin for **Pro Evolution Soccer 6** that surgically intercepts the game's internal physics integrator to fix one of the most frustrating issues in the original game: weak, lifeless first-touch passes. By hooking two precise offsets in `pes6.exe` and applying distance-aware power rescaling algorithms, the mod restores the inertia your passes deserve.

<CardGroup cols={2}>
  <Card title="Introduction" icon="flag" href="/introduction">
    Learn what the mod does, why it was built, and how it fits into the PES6 modding ecosystem.
  </Card>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Build and deploy the DLL in minutes. Get the mod running inside PES6.
  </Card>
  <Card title="Architecture" icon="sitemap" href="/architecture/overview">
    Understand the dual-hook system, EDI interception pipeline, and module layout.
  </Card>
  <Card title="Configuration" icon="sliders" href="/configuration/pass-power-config">
    Tune inertia bands, boost lookup tables, soft floors, and awkward-angle rescue thresholds.
  </Card>
</CardGroup>

## How it works

The mod injects two JMP hooks into `pes6.exe` at startup:

<Steps>
  <Step title="Context Hook — capture passer & receiver">
    At `pes6.exe+1A5905`, the hook fires every time a pass is initiated. It records the passer's and receiver's memory pointers and computes the discrete cell distance between them.
  </Step>
  <Step title="Power Hook — intercept EDI before application">
    At `pes6.exe+1A637B`, the hook fires just before the pass power value (held in the `EDI` register) is written to the ball. `CalculateModifiedEDI()` reads the context, selects the correct boost from lookup tables, and returns a rescaled value.
  </Step>
  <Step title="Geometry analysis — dot-product rescue">
    For awkward passes (ball behind the passer, tight angles), the mod reads world-space coordinates and uses a dot-product check to apply additional compensating boosts.
  </Step>
  <Step title="Runtime toggle — Ctrl+Shift+P">
    Press `Ctrl+Shift+P` at any time to toggle the mod on or off without restarting the game. An optional Kitserver overlay confirms the state on screen.
  </Step>
</Steps>

## Key features

<CardGroup cols={2}>
  <Card title="Distance-Aware Boosting" icon="ruler">
    Pass power is scaled against discrete cell distance and real Euclidean geometry. Short, medium, and long passes each receive appropriate compensation.
  </Card>
  <Card title="Inertia Band Classification" icon="chart-bar">
    Ball inertia at the moment of the pass is classified into five bands (ZERO → HIGH) so the boost tables can apply the right correction for every gameplay situation.
  </Card>
  <Card title="Awkward-Angle Rescue" icon="compass">
    Dot-product geometry detects when the ball is on the wrong side of the passer and applies surgical extra boosts to fix passes that the original engine botches.
  </Card>
  <Card title="Safe Memory Patching" icon="shield-check">
    Before writing any hook, `CheckBytes()` validates that the target bytes match the expected original opcodes. If the game version is wrong, hooks are aborted cleanly.
  </Card>
  <Card title="Kitserver Overlay" icon="display">
    Integrates with Kitserver's `kload.dll` at runtime via dynamic binding. If Kitserver is not present, the mod continues to work silently.
  </Card>
  <Card title="Structured Debug Logging" icon="file-lines">
    Every pass is logged with full metrics: passer/receiver addresses, cell coordinates, EDI before and after, boost mode, geometry dot product, and more.
  </Card>
</CardGroup>

<Note>
  The mod targets the **unmodified retail release of PES6 (Win32)**. Hook addresses are validated against expected byte sequences before installation, so loading into an incompatible build will fail gracefully with log output.
</Note>
